{
  "name": "techmash",
  "version": "1.0.0",
  "description": "Ranking languages based on the ratings they get from users",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "NODE_ENV=production node backend/index.js",
    "dev": "NODE_ENV=development nodemon backend/index.js",
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Amaan Khan (bornengineer)",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "fs": "^0.0.1-security",
    "joi": "^17.13.3",
    "path": "^0.12.7",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
